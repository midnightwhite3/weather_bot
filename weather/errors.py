class StatusError(Exception): pass
    # def __init__(self, msg="Something is wrong on the server side. Try again later."):
    #     self.message = msg
    #     super().__init__(self.message)

ERRORS = {
    
        }